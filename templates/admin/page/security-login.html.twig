{% extends '@SoCore/layout-authentication.html.twig' %}

{% block layoutContents %}
	
	<div class="row justify-content-center">
		<div class="col-lg-5 pt-5">
			{% for flash_message in app.flashes('auth_success') %}
				<div class="alert alert-success mb-3" role="alert">{{ flash_message }}</div>
			{% endfor %}
			
			<div class="card shadow-lg border-0 rounded-lg">
				<div class="card-header">
					<h3 class="text-center font-weight-light my-4">{{ 'page.admin_login.title' | trans([], 'admin') }}</h3>
				</div>
				<div class="card-body">
					{% if login.error %}
						<div class="alert alert-warning alert-dismissible fade show" role="alert">
							<div class="d-flex align-items-center">
								<span class="me-2">
									<i class="fa fa-exclamation-triangle"></i>
								</span>
								<div>
									{{ login.error.messageKey | trans(login.error.messageData) }}
								</div>
							</div>
							<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'action.close' | trans }}"></button>
						</div>
					{% endif %}
					
					<form method="post">
					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
					<input name="isAdmin" type="hidden" value="1"/>
					<div class="form-floating mb-3">
						<input id="InputLoginEmail" name="_username" type="email" class="form-control" required autocomplete="username"
							   placeholder="{{ 'page.admin_login.email' | trans([], 'admin') }}" {{ login.email ? 'value="%s"'|format(login.email)|raw : '' }}/>
						<label for="InputLoginEmail">{{ 'page.admin_login.email' | trans([], 'admin') }}</label>
					</div>
					<div class="form-floating mb-3">
						<input id="InputLoginPassword" name="_password" type="password" class="form-control" autocomplete="current-password" required
							   placeholder="{{ 'page.admin_login.password' | trans([], 'admin') }}"/>
						<label for="InputLoginPassword">{{ 'page.admin_login.password' | trans([], 'admin') }}</label>
					</div>
					{#                    <div class="form-check mb-3"> #}
					{#                        <input class="form-check-input" id="inputRememberPassword" type="checkbox" value=""> #}
					{#                        <label class="form-check-label" for="inputRememberPassword">Remember Password</label> #}
					{#                    </div> #}
					<div class="d-flex align-items-center justify-content-between mt-4 mb-0">
						<a class="small" href="password.html">Forgot Password?</a>
						<button type="submit" class="btn btn-primary">{{ 'action.login' | trans }}</button>
					</div>
					</form>
				</div>
				<div class="card-footer text-center py-3">
					<div class="small"><a href="{{ path('admin_register') }}">{{ 'page.admin_login.registerAction' | trans([], 'admin') }}</a></div>
				</div>
			</div>
		</div>
	</div>
	
	{#
	<div id="app" class="app">
		
		<div class="login login-v2 fw-bold">
			
			<div class="login-container">
				
				<div class="login-header">
					<div class="brand">
						<div class="d-flex align-items-center pe-1">
							<span class="logo"></span>
							{{ 'admin.label' | trans({'app': '<b>' ~ ('app.label' | trans) ~ '</b>'}, 'admin') | raw }}
						</div>
						<small>{{ 'page.login.legend' | trans([], 'admin') }}</small>
					</div>
					<div class="icon">
						<i class="fa fa-lock"></i>
					</div>
				</div>
				
				<div class="login-content">
					{% if login.error %}
						<div class="alert alert-warning alert-dismissible fade show" role="alert">
							<div class="d-flex align-items-center">
								<span class="me-2">
									<i class="fa fa-exclamation-triangle"></i>
								</span>
								<div>
									{{ login.error.messageKey | trans(login.error.messageData) }}
								</div>
							</div>
							<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'action.close' | trans }}"></button>
						</div>
					{% endif %}
					
					<form action="{{ path('login') }}" method="post">
					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
					<input name="isAdmin" type="hidden" value="1"/>
					<div class="form-floating mb-3">
						<input id="InputLoginEmail" name="adminEmail" type="text" class="form-control" required autocomplete="username"
							   placeholder="{{ 'user.field.email' | trans }}" {{ login.email ? 'value="%s"'|format(login.email)|raw : '' }}/>
						<label for="InputLoginEmail" class="d-flex align-items-center text-gray-600 fs-13px">{{ 'user.field.email' | trans }}</label>
					</div>
					<div class="form-floating mb-3">
						<input id="InputLoginPassword" name="adminPassword" type="password" class="form-control" autocomplete="current-password" required
							   placeholder="{{ 'user.field.password' | trans }}"/>
						<label for="InputLoginPassword" class="d-flex align-items-center text-gray-600 fs-13px">{{ 'user.field.password' | trans }}</label>
					</div>
					<div class="d-grid pt-2 mb-3">
						<button type="submit" class="btn btn-success btn-lg">{{ 'user.login.loginAction' | trans }}</button>
					</div>
					</form>
				</div>
			
			</div>
		</div>
	</div>
	#}
{% endblock %}
